
"use client";

// This file can be deleted as React Quill is being removed.
// Placeholder content to avoid build errors if still imported somewhere.

export default function RichTextEditor() {
  return null; 
}
